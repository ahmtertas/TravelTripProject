@model List<TravelTripProject.Models.Classes.Blog>
<div class="box1-left">
    @foreach(var item in Model) {
    <div class="bot-img">
        <img src="@item.BlogImage" alt="" style="width:120px; height:60px;"/>
    </div>
    <div class="bot-desc1">
        <p>@item.Aciklama.Substring(0,60)</p>
    </div>
    <div class="clear"></div>
    }
</div>
